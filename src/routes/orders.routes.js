import { Router } from "express";

const router = Router();

// router.route("/").get();
// router.route("/").post();
// router.route("/:id").get();
// router.route("/:id/cancel").put();
// router.route("/:id/status").put();

export default router;
